# FinAlze API service dependencies (slim — NO ML stack).
# The heavy extraction deps (torch/docling/surya/…) stay in requirements.txt and are
# only installed in the worker image. Install for API dev:  pip install -r requirements-api.txt

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
fastapi-users[sqlalchemy]>=14.0
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
aiosqlite>=0.20            # async SQLite for tests/integration
alembic>=1.13
celery[redis]>=5.4
redis>=5.0
flower>=2.0
pyjwt[crypto]>=2.9
pwdlib[argon2]>=0.2
fastapi-limiter>=0.1.6
python-multipart>=0.0.9
httpx>=0.27
prometheus-fastapi-instrumentator>=7.0
minio>=7.2
